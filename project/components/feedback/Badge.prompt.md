Badge — compact pill for status (Title verified, Pending), or metadata (acreage, zoning). Use `tone="success"` + `dot` for trust signals.

```jsx
<Badge tone="success" dot>Title verified</Badge>
<Badge tone="forest">42 acres</Badge>
```
